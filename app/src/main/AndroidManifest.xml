<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.eladoktarizo.monitoring_belajar">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label=""
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PilihanLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginGuru"/>
        <activity android:name=".MenuGuru" />
        <activity android:name=".LoginOrtu" />
        <activity android:name=".RegisterOrtu" />
        <activity android:name=".MenuOrtu" />
        <activity android:name=".DataAnak"></activity>
    </application>

</manifest>